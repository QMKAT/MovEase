<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.movease">

    <!-- Internet for Firebase, maps, mock API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovEase"
        tools:targetApi="31">

        <!-- Splash screen as launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MovEase">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />

        <!-- Role‑based home screens -->
        <activity android:name=".MoverHomeActivity" />
        <activity android:name=".ProviderHomeActivity" />
        <activity android:name=".MoverBookingsActivity" />

        <!-- Plan list & details -->
        <activity android:name=".PlanListActivity" />

        <!-- Free map view -->
        <activity android:name=".MapActivity" />

        <activity android:name=".PlanDetailActivity" />
        <activity android:name=".OrdersActivity" />
        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- Optional: default notification channel (not mandatory, but keeps things tidy) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="movease_general" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_truck" />
    </application>

</manifest>